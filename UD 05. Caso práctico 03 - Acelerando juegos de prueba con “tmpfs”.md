# UD 05. Caso práctico 03 - Acelerando juegos de prueba con “tmpfs”

# 1: Crear contenedor MySQL SIN tmpfs

Ejecutamos el siguiente comando:



# 2: Crear contenedor MySQL CON tmpfs

Ahora ejecutamos:



# 3: Acceder a los contenedores

Sin tmpfs:

docker exec -it mysqlsintmpfs bash

Con tmpfs:

docker exec -it mysqlcontmpfs bash

# 4: Entrar en MySQL

Dentro del contenedor:

mysql -u root

# 5: Realizar prueba de rendimiento

Ejecutamos:

USE test;

SELECT SQL_NO_CACHE * FROM posts;

